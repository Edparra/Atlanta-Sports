<h1>Teams</h1>

<ul style="list-style-type: none;">
	<% @teams.each do |team| %>
		<li class="group">
			<img class="logo" src="<%= team.logo_url %>"> <a href="/teams/<%= team.id %>"><%= team.team_name %></a>
		</li>
	<% end %>
</ul>

<a href="/teams/add">Add Team</a>
